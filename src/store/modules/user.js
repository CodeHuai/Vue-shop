import { getUserTempId } from "@/utils/userabout";

const state = {
  userTempId: getUserTempId(),
  // userTempId: Date.now(),
};

const mutations = {};

const actions = {};

const getters = {};

export default {
  state,
  mutations,
  actions,
  getters,
};
